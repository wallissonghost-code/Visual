import fs from 'node:fs';import path from 'node:path';import {execFileSync} from 'node:child_process';import {runStaticAudit} from './static-audit.js';import {runSecurityAudit} from './security-audit.js';import {runBrowserAudit} from './browser-audit.js';import {writeReport} from './report.js';
const argv=process.argv.slice(2),args={};for(let i=0;i<argv.length;i++)if(argv[i].startsWith('--'))args[argv[i].slice(2)]=argv[i+1]?.startsWith('--')?true:argv[++i];const target=args.target||process.env.VISUAL_TARGET,mode=args.mode||(target?.includes('github.com')?'repo':'url'),profile=args.profile||'visual';if(!target){console.error('Uso: --target <repo|URL> --mode repo|url --profile visual|security');process.exit(2)}const work=path.resolve('.visual-work');fs.mkdirSync(work,{recursive:true});let findings=[],meta={target,mode,profile,startedAt:new Date().toISOString()};try{if(mode==='repo'){let root=target;if(/^https?:\/\/github\.com\//.test(target)){root=path.join(work,'target');fs.rmSync(root,{recursive:true,force:true});execFileSync('git',['clone','--depth=1',target,root],{stdio:'inherit'})}findings.push(...(profile==='security'?runSecurityAudit(root):runStaticAudit(root)));meta.note=profile==='security'?'Auditoria de segurança inspirada no laboratório do repositório Modelo: procura riscos de licença, sessão, device binding, transporte, armazenamento e exposição de capabilities. Não executa brute force.':'Auditoria visual por código: funciona mesmo quando a página publicada exige autenticação.'}else{findings.push(...await runBrowserAudit(target,work));meta.note=profile==='security'?'Auditoria pública/passiva da página. Testes destrutivos, brute force e uso de credenciais não são executados automaticamente.':'Auditoria dinâmica: navega, clica, compara estados e testa viewports.'}}catch(error){findings.push({severity:'critical',bot:'runner',rule:'AUDIT_CRASH',file:'-',message:error.message,recommendation:'Verifique acesso, dependências e configuração do alvo.',category:'Execução',confidence:100})}const out=writeReport({meta,findings});console.log(`Relatório: ${out}`);process.exit(findings.some(f=>f.severity==='critical')?1:0);