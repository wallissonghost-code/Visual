import fs from 'node:fs';import path from 'node:path';import {execFileSync} from 'node:child_process';import {runStaticAudit} from './static-audit.js';import {runStructuralAudit} from './visual/structural-audit.js';import {runSecurityAudit} from './security-audit.js';import {runBrowserAudit} from './browser-audit.js';import {writeReport} from './report.js';import {visualBuildInfo} from './version.js';
const argv=process.argv.slice(2),args={};for(let i=0;i<argv.length;i++)if(argv[i].startsWith('--'))args[argv[i].slice(2)]=argv[i+1]?.startsWith('--')?true:argv[++i];const target=args.target||process.env.VISUAL_TARGET,mode=args.mode||(target?.includes('github.com')?'repo':'url'),profile=args.profile||'visual';if(!target){console.error('Uso: --target <repo|URL> --mode repo|url --profile visual|security [--report arquivo] [--json-report arquivo]');process.exit(2)}const work=path.resolve('.visual-work',args.job||'default');fs.mkdirSync(work,{recursive:true});let findings=[],meta={target,mode,profile,startedAt:new Date().toISOString(),...visualBuildInfo()};try{if(mode==='repo'){let root=target;if(/^https?:\/\/github\.com\//.test(target)){root=path.join(work,'target');fs.rmSync(root,{recursive:true,force:true});execFileSync('git',['clone','--depth=1',target,root],{stdio:'pipe'})}if(profile==='security'){findings.push(...runSecurityAudit(root));meta.note='Auditoria de segurança por código.'}else{findings.push(...runStaticAudit(root),...runStructuralAudit(root));meta.note='Visual Bot: auditoria por código com heurísticas visuais e análise estrutural de duplicação, sobreposição e código potencialmente morto.'}}else{findings.push(...await runBrowserAudit(target,work));meta.note=profile==='security'?'Auditoria pública/passiva da página.':'Auditoria dinâmica: navega, clica, compara estados e testa viewports.'}}catch(error){findings.push({severity:'critical',bot:'runner',rule:'AUDIT_CRASH',file:'-',message:error.message,recommendation:'Verifique acesso, dependências e configuração do alvo.',category:'Execução',confidence:100,evidenceLevel:'confirmado'})}meta.finishedAt=new Date().toISOString();const out=writeReport({meta,findings,outputPath:args.report});if(args['json-report']){const jsonOut=path.resolve(args['json-report']);fs.mkdirSync(path.dirname(jsonOut),{recursive:true});const crashed=findings.some(f=>f.rule==='AUDIT_CRASH');fs.writeFileSync(jsonOut,JSON.stringify({schemaVersion:1,visualBot:{version:meta.version,build:meta.build,module:meta.module},audit:{target:meta.target,mode:meta.mode,profile:meta.profile,startedAt:meta.startedAt,finishedAt:meta.finishedAt,status:crashed?'failed':'completed'},summary:{total:findings.length,confirmed:findings.filter(f=>f.evidenceLevel==='confirmado').length,strong:findings.filter(f=>f.evidenceLevel==='forte').length,suspected:findings.filter(f=>!f.evidenceLevel||f.evidenceLevel==='suspeita').length,critical:findings.filter(f=>f.severity==='critical').length,high:findings.filter(f=>f.severity==='high').length,medium:findings.filter(f=>f.severity==='medium').length,low:findings.filter(f=>f.severity==='low').length},findings},null,2))}console.log(`Relatório: ${out}`);process.exit(findings.some(f=>f.severity==='critical')?1:0);