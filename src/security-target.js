import fs from 'node:fs';import path from 'node:path';import {execFileSync} from 'node:child_process';
const TEXT=new Set(['.js','.mjs','.cjs','.ts','.tsx','.jsx','.html','.json','.yml','.yaml','.md']);
function walk(root,out=[]){for(const e of fs.readdirSync(root,{withFileTypes:true})){if(['.git','node_modules','dist','build','.next'].includes(e.name))continue;const p=path.join(root,e.name);if(e.isDirectory())walk(p,out);else if(TEXT.has(path.extname(e.name)))out.push(p)}return out}
function discover(root){let all='';for(const f of walk(root)){try{all+='\n'+fs.readFileSync(f,'utf8')}catch{}}const ws=all.match(/wss:\/\/[A-Za-z0-9._:-]+(?:\/[A-Za-z0-9._~!$&'()*+,;=:@%\/-]*)?/i)?.[0]||'';const pa=all.match(/https:\/\/[A-Za-z0-9._:-]+\/api\/licenses\/validate/i)?.[0]||'';const game=all.match(/(?:GAME_ID|gameId)\s*[:=]\s*['"]([A-Za-z0-9._-]+)['"]/i)?.[1]||'';return{relayUrl:ws,licenseUrl:pa,gameId:game}}
export function resolveSecurityTarget(target,id='target'){if(/^https:\/\/github\.com\//i.test(target)){const root=path.resolve('.visual-work',`security-${id}`);fs.rmSync(root,{recursive:true,force:true});execFileSync('git',['clone','--depth=1',target,root],{stdio:'pipe',timeout:60000});return{kind:'repo',target,...discover(root)}}const u=new URL(target);return{kind:'url',target,relayUrl:`${u.protocol==='https:'?'wss':'ws'}://${u.host}/relay`,licenseUrl:'',gameId:''}}
