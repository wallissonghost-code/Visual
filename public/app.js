let mode='repo';const $=s=>document.querySelector(s);document.querySelectorAll('.mode').forEach(b=>b.onclick=()=>{document.querySelectorAll('.mode').forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.mode;$('#target').placeholder=mode==='repo'?'https://github.com/owner/projeto.git':'https://seu-site.com';});$('#run').onclick=async()=>{const target=$('#target').value.trim();if(!target)return alert('Cole o link que deseja analisar.');$('#status').classList.remove('hidden');$('#result').classList.add('hidden');$('#state').textContent='Bots analisando…';$('#detail').textContent=mode==='repo'?'Lendo código, estados e regras responsivas.':'Testando a interface em diferentes viewports.';try{const r=await fetch('/api/audits',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({target,mode})});const j=await r.json();if(!r.ok)throw Error(j.error||'Falha ao iniciar');poll(j.id);}catch(e){$('#state').textContent='Não foi possível iniciar';$('#detail').textContent=e.message;}};async function poll(id){const r=await fetch('/api/audits/'+id),j=await r.json();if(j.status==='completed'||j.status==='failed'){ $('#status').classList.add('hidden');$('#result').classList.remove('hidden');$('#badge').textContent=j.status==='completed'?'CONCLUÍDO':'FALHOU';$('#report').textContent=j.report||j.output||'Sem relatório.';return;}setTimeout(()=>poll(id),1200)}